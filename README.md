# CryptoCraft Marketplace: Полное руководство

> Etsy-подобный маркетплейс для уникальных товаров ручной работы и коллекционных предметов с глубокой интеграцией AI, криптовалютными платежами через блокчейн TON и мощными социальными функциями.

**CryptoCraft** — это не просто торговая площадка, а целая экосистема, созданная для поддержки ремесленников, коллекционеров и ценителей уникальных вещей. Мы объединяем традиционное мастерство с передовыми технологиями, чтобы предоставить пользователям безопасный, удобный и увлекательный опыт покупки и продажи.

---

## 📜 Оглавление

1.  [✨ Ключевые особенности](#-ключевые-особенности)
    *   [1.1. AI-ассистент продавца](#11-ai-ассистент-продавца)
    *   [1.2. Блокчейн и платежи (TON)](#12-блокчейн-и-платежи-ton)
    *   [1.3. E-commerce функционал](#13-e-commerce-функционал)
    *   [1.4. Социальные и комьюнити функции](#14-социальные-и-комьюнити-функции)
    *   [1.5. Панель продавца](#15-панель-продавца)
    *   [1.6. Административная панель](#16-административная-панель)
2.  [🛠️ Технологический стек](#️-технологический-стек)
3.  [🏛️ Архитектура](#️-архитектура)
    *   [3.1. Структура монорепозитория](#31-структура-монорепозитория)
    *   [3.2. Архитектура Backend (Модульный монолит)](#32-архитектура-backend-модульный-монолит)
    *   [3.3. Схема взаимодействия](#33-схема-взаимодействия)
4.  [🚀 Локальный запуск](#-локальный-запуск)
    *   [4.1. Предварительные требования](#41-предварительные-требования)
    *   [4.2. Установка и запуск](#42-установка-и-запуск)
5.  [⚙️ Конфигурация `.env` (Переменные окружения)](#️-конфигурация-env-переменные-окружения)
    *   [5.1. Корневой Frontend (`/.env`)](#51-корневой-frontend-env)
    *   [5.2. Backend (`/backend/.env`)](#52-backend-backendenv)
6.  [📁 Структура проекта](#-структура-проекта)
7.  [🧠 Ключевые концепции](#-ключевые-концепции)
    *   [7.1. AI-интеграция](#71-ai-интеграция)
    *   [7.2. Платежная система (TON)](#72-платежная-система-ton)
    *   [7.3. Аутентификация товаров и NFT-сертификаты](#73-аутентификация-товаров-и-nft-сертификаты)
    *   [7.4. Система споров (Арбитраж)](#74-система-споров-арбитраж)
    *   [7.5. Управление DAO](#75-управление-dao)
8.  [✨ Недавние улучшения](#-недавние-улучшения)
9.  [🗺️ Дорожная карта (Roadmap)](#️-дорожная-карта-roadmap)
10. [✅ Готово / 🔧 В работе / 🧩 Запланировано](#-готово--в-работе--запланировано)
11. [🛡️ Безопасность и соответствие](#️-безопасность-и-соответствие)
12. [🤝 Вклад и правила PR](#-вклад-и-правила-pr)
13. [📚 Журнал решений (ADR-lite)](#-журнал-решений-adr-lite)
14. [❓ FAQ по разработке](#-faq-по-разработке)

---

## ✨ Ключевые особенности

Проект реализует широкий спектр функций, превращая его в полномасштабную e-commerce платформу.

### 1.1. AI-ассистент продавца

Интеграция с **Google Gemini API** для упрощения и автоматизации рутинных задач.

*   **Генерация листингов:** AI анализирует фотографию и краткое текстовое описание товара, после чего автоматически генерирует:
    *   Привлекательное, SEO-оптимизированное название.
    *   Подробное, структурированное описание.
    *   Рекомендованную рыночную цену.
    *   Наиболее подходящую категорию и подкатегорию.
    *   Значения для динамических атрибутов категории (например, "Материал: Керамика", "Бренд: Apple").
*   **AI Фотостудия:** Встроенный редактор изображений, позволяющий продавцам улучшать свои фотографии с помощью текстовых подсказок. Функции включают:
    *   Удаление или замена фона.
    *   Улучшение освещения и цветокоррекция.
    *   Добавление объектов или изменение окружения.
*   **Импорт товаров:** Уникальная функция, которая позволяет продавцам импортировать свои товары с других площадок (например, OLX, Prom.ua, Etsy). Пользователь вставляет ссылку, система с помощью AI парсит страницу, извлекает всю информацию (название, описание, цена, фото) и создает готовый черновик объявления в CryptoCraft.
*   **Аналитика и советы:** AI анализирует данные из панели продавца и предоставляет персонализированные рекомендации по увеличению продаж, оптимизации цен и улучшению листингов.

### 1.2. Блокчейн и платежи (TON)

Полная интеграция с блокчейном **The Open Network (TON)** для быстрых и дешевых транзакций.

*   **Подключение кошелька:** Бесшовная интеграция с TON-кошельками через **TON Connect**, что позволяет пользователям легко подключаться и проводить транзакции.
*   **Безопасные сделки (Эскроу):** Основной метод оплаты. Средства покупателя замораживаются на специальном кошельке платформы (`TREASURY_WALLET_ADDRESS`) и переводятся продавцу только после того, как покупатель подтвердит получение и соответствие товара. Это гарантирует безопасность для обеих сторон.
*   **Прямые платежи:** Для верифицированных PRO-продавцов доступна опция получения оплаты напрямую на их личный TON-кошелек, минуя эскроу-сервис платформы.

### 1.3. E-commerce функционал

Все необходимые инструменты для современной электронной коммерции.

*   **Real-time чат:** Полноценный мессенджер между покупателями и продавцами с поддержкой отправки изображений, контекста товара (карточка товара прямо в чате) и быстрых ответов.
*   **Система споров (Арбитраж):** Если возникает проблема с заказом, любая из сторон может открыть спор. Открывается специальный чат с участием покупателя, продавца и администратора платформы, который выступает в роли арбитра.
*   **Постдоставка отзывы:** Оценки можно оставить только после статусов *Delivered/Completed*. Поддерживаются фото и видео, считается fraud score, ведутся поведенческие сигнатуры, токсичный контент автоматически скрывается до модерации.
*   **Авто PRO-статус:** Продавцы получают/теряют PRO автоматически — нужно ≥20 отзывов 5★ и хотя бы одна новая оценка каждые 30 дней, иначе статус снимается без участия модераторов.
*   **Аутентификация товаров:** Для дорогостоящих товаров (электроника, ювелирные изделия) доступна услуга проверки подлинности. Товар отправляется в экспертный центр CryptoCraft, где специалисты проверяют его состояние и оригинальность. После успешной проверки товару присваивается специальный значок, а также может быть выпущен **NFT-сертификат подлинности**.
*   **Вариации товаров:** Продавцы могут создавать товары с различными вариантами (например, одежда разных размеров и цветов), каждый со своей ценой, количеством и фотографией.

### 1.4. Социальные и комьюнити функции

Превращение маркетплейса в живое сообщество.

*   **Live-трансляции:** PRO-продавцы запускают эфиры через LiveKit с защищённой выдачей токенов, системой жалоб, записью эфиров и аналитикой пиковых зрителей.
*   **Мастерская (Workshop):** Социальная лента с медиа-постами, лайками, комментариями и жалобами; модераторы видят флаги и могут скрывать спорный контент.
*   **Форум:** Полноценный форум с поиском, закрепами, пагинацией и возможностью закрывать темы; пользователи могут репортить токсичные ответы.
*   **Управление DAO:** PRO-пользователи получают право голоса и могут участвовать в управлении развитием платформы, голосуя за предложения (например, изменение комиссии, добавление новых функций).

### 1.5. Панель продавца

Мощный инструмент для управления своим бизнесом на платформе.

*   **Продвинутая аналитика:** Визуальные графики и отчеты по продажам, просмотрам, конверсии, источникам трафика и самым популярным товарам.
*   **Управление промокодами:** Создание и управление персональными промокодами (процентные или фиксированные скидки).
*   **Финансовая сводка:** Обзор баланса, ожидаемых поступлений и задолженности по комиссии.

### 1.6. Административная панель

Отдельное, полнофункциональное приложение для полного контроля над платформой.

*   **Управление пользователями:** Просмотр, редактирование, блокировка пользователей.
*   **Модерация контента:** Одобрение или отклонение новых товаров, проверка жалоб.
*   **Финансовый контроль:** Мониторинг всех транзакций на платформе.
*   **Управление категориями:** Создание и редактирование структуры категорий и их атрибутов.
*   **Глобальные настройки:** Управление системными параметрами, такими как размер комиссии.

---

## 🛠️ Технологический стек

| Компонент                | Технологии                                                                  |
| ------------------------ | --------------------------------------------------------------------------- |
| **Основной Frontend**      | React, Vite, TypeScript, React Router, TailwindCSS, DaisyUI                 |
| **Панель Администратора**    | React, Vite, TypeScript, React Router, TailwindCSS                         |
| **Backend**                | NestJS (Модульный монолит), TypeORM, PostgreSQL, Class-Validator          |
| **Real-time & WebSockets** | Socket.IO, LiveKit (для live-трансляций)                                    |
| **Аутентификация**       | JWT, Telegram Web App (для основного приложения), Email/Password (для админки) |
| **Блокчейн**             | TON Connect (`@tonconnect/ui-react`)                                        |
| **Внешние API**          | Google Gemini API (для AI), Cloudinary (хранение медиа), Nova Poshta API    |

---

## 🏛️ Архитектура

Проект построен по принципу **монорепозитория** и разделен на три независимых, но взаимосвязанных приложения, что обеспечивает четкое разделение ответственности и удобство разработки.

### 3.1. Структура монорепозитория

```
/
├── admin/              # Frontend-приложение для панели администратора
├── backend/            # Backend-приложение на NestJS
├── node_modules/       # Зависимости основного frontend-приложения
├── public/
├── src/                # Исходный код основного frontend-приложения
├── package.json        # package.json для основного frontend-приложения
└── ...                 # Прочие файлы конфигурации
```

*   **Основное приложение (`/`)**: Клиентская часть для покупателей и продавцов на React.
*   **Панель администратора (`/admin`)**: Полностью автономное React-приложение для управления платформой.
*   **Бэкенд (`/backend`)**: Серверная часть на NestJS.

### 3.2. Архитектура Backend (Модульный монолит)

Вместо реализации сложных микросервисов, бэкенд построен как **модульный монолит**. Это означает, что вся бизнес-логика находится в одном приложении, но разделена на четкие, слабо связанные модули по доменной области (`UsersModule`, `ProductsModule`, `OrdersModule`, `ChatsModule` и т.д.).

**Преимущества такого подхода:**
*   **Простота разработки и развертывания:** Нет необходимости управлять сложной инфраструктурой микросервисов.
*   **Высокая производительность:** Внутримодульное взаимодействие происходит напрямую, без сетевых задержек.
*   **Хорошая структурированность:** Код остается чистым и организованным, что облегчает его поддержку и масштабирование в будущем.

### 3.3. Схема взаимодействия

Клиентские приложения (Frontend и Admin Panel) взаимодействуют с бэкендом через единый **REST API**. Для функционала реального времени (чаты, уведомления, live-трансляции) используется **WebSocket (Socket.IO)**.

---

## 🚀 Локальный запуск

### 4.1. Предварительные требования

*   **Node.js** (рекомендуется версия v18.x или выше)
*   **npm** или **pnpm**
*   **PostgreSQL** (локально или через Docker)
*   Аккаунты в **Cloudinary**, **Google AI Studio** (для Gemini API), **Telegram** (@BotFather).

### 4.2. Установка и запуск

Процесс запуска включает в себя три одновременных шага для каждого из приложений.

**1. Клонирование репозитория:**
```bash
git clone <your-repository-url>
cd <your-repository-name>
```

**2. Настройка Backend:**

*   Перейдите в директорию бэкенда:
    ```bash
    cd backend
    ```
*   Установите зависимости:
    ```bash
    npm install
    ```
*   Создайте файл `.env` в папке `/backend` на основе `backend/env.txt` и заполните все необходимые переменные (см. [раздел конфигурации](#️-конфигурация-env-переменные-окружения)).
*   Запустите сервер в режиме разработки:
    ```bash
    npm run start:dev
    ```
*   Бэкенд будет доступен по адресу `http://localhost:3001`.

**3. Настройка основного Frontend:**

*   Вернитесь в корневую директорию проекта:
    ```bash
    cd ..
    ```
*   Установите зависимости:
    ```bash
    npm install
    ```
*   Создайте файл `.env` в **корневой папке проекта** на основе `env.txt` и укажите `VITE_API_BASE_URL`.
*   Запустите frontend-приложение:
    ```bash
    npm run dev
    ```
*   Основное приложение будет доступно по адресу `http://localhost:3000`.

**4. Настройка Admin Panel:**

*   Перейдите в директорию админ-панели:
    ```bash
    cd admin
    ```
*   Установите зависимости:
    ```bash
    npm install
    ```
*   Создайте файл `.env` в папке `/admin` на основе `admin/env.txt` (если он нужен для специфичных ключей).
*   Запустите админ-панель:
    ```bash
    npm run dev
    ```
*   Админ-панель будет доступна по адресу `http://localhost:5173`.

---

## ⚙️ Конфигурация `.env` (Переменные окружения)

Для корректной работы системы необходимо настроить файлы переменных окружения.

### 5.1. Корневой Frontend (`/.env`)
```env
# URL вашего бэкенда
# Для локальной разработки:
VITE_API_BASE_URL=http://localhost:3001
# Для продакшена (это значение нужно установить в переменных окружения на Render):
# VITE_API_BASE_URL=https://your-backend-url.onrender.com

# API ключ Gemini для frontend-сервисов (если необходимо)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY
```

### 5.2. Backend (`/backend/.env`)
```env
# --- База данных ---
# Строка подключения к вашей PostgreSQL базе данных.
# Формат: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=

# --- Аутентификация ---
# Секретный ключ для подписи JWT токенов. Придумайте любую длинную и сложную строку.
JWT_SECRET=

# Токен вашего Telegram-бота. Получите его у @BotFather в Telegram.
TELEGRAM_BOT_TOKEN=

# --- Внешние сервисы ---
# API ключ для Google Gemini. Получите его в Google AI Studio.
API_KEY=

# URL для подключения к сервису Cloudinary (для загрузки медиафайлов).
# Найдите его в вашей панели управления Cloudinary.
# Формат: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
CLOUDINARY_URL=

# --- Загрузка медиа ---
# (опционально) Переопределите лимиты, если нужно.
# По умолчанию: 8 МБ на изображение, авто-ресайз до 1920px, видео до 60 МБ.
UPLOAD_MAX_IMAGE_SIZE_BYTES=8388608
UPLOAD_MAX_IMAGE_DIMENSION=1920
UPLOAD_MAX_VIDEO_SIZE_BYTES=62914560

# API ключ для сервиса "Нова Пошта". Получите его в личном кабинете Новой Пошты.
NOVA_POSHTA_API_KEY=

# --- LiveKit (для live-трансляций) ---
# Получите ключи в панели управления LiveKit Cloud.
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# --- CORS ---
# URL-адреса клиентских приложений для настройки CORS.
# Для локальной разработки (оставьте эти значения):
FRONTEND_URL=http://localhost:3000
ADMIN_URL=http://localhost:5173
# Для продакшена (укажите реальные URL ваших развернутых приложений):
# FRONTEND_URL=https://cryptocraft-fronted.onrender.com
# ADMIN_URL=https://administrator-zoai.onrender.com

# --- Данные для входа в админ-панель (для супер-администратора) ---
SUPER_ADMIN_EMAIL=admin
SUPER_ADMIN_PASSWORD=admin
```

---

## 📁 Структура проекта

Краткий обзор ключевых директорий и их назначения.

```
/
├── admin/                  # Исходный код админ-панели
│   ├── components/
│   ├── pages/
│   ├── services/           # Сервисы для взаимодействия с API
│   └── hooks/
├── backend/                # Исходный код бэкенда
│   └── src/
│       ├── ai/             # Модуль для работы с Gemini API
│       ├── auth/           # Модуль аутентификации (JWT, Telegram)
│       ├── categories/     # Модуль управления категориями
│       ├── chats/          # Модуль чатов
│       ├── orders/         # Модуль заказов
│       ├── products/       # Модуль товаров
│       ├── users/          # Модуль пользователей
│       └── ...             # Прочие модули (disputes, governance, etc.)
└── src/                    # Исходный код основного frontend-приложения
    ├── components/         # Переиспользуемые UI-компоненты
    ├── pages/              # Компоненты-страницы
    ├── services/           # Сервисы для взаимодействия с API (apiService, geminiService)
    ├── hooks/              # Пользовательские хуки (useAuth, useCart, etc.)
    ├── lib/                # Вспомогательные утилиты
    └── types.ts            # Глобальные определения типов TypeScript
```

---

## 🧠 Ключевые концепции

### 7.1. AI-интеграция

Все запросы к AI инкапсулированы на бэкенде. Исторически использовался **Google Gemini API**; стратегия — миграция на **DeepSeek** для снижения стоимости. Frontend-приложения отправляют запросы на эндпоинты бэкенда (например, `/ai/generate-listing`), а бэкенд безопасно выполняет запрос к выбранному провайдеру, используя ключи из ENV. Это защищает ключи от раскрытия на стороне клиента и позволяет гибко менять провайдера.

### 7.2. Платежная система (TON)

*   **Эскроу (Безопасная сделка):**
    1.  Покупатель отправляет средства на кошелек платформы (`TREASURY_WALLET_ADDRESS`).
    2.  Бэкенд фиксирует поступление средств и меняет статус заказа на `PAID`.
    3.  Продавец отправляет товар.
    4.  Покупатель подтверждает получение, и бэкенд инициирует перевод средств с кошелька платформы на кошелек продавца (за вычетом комиссии).
*   **Прямая оплата:**
    1.  Покупатель отправляет средства напрямую на `tonWalletAddress` продавца.
    2.  Эта транзакция происходит вне контроля платформы. Платформа лишь предоставляет интерфейс для инициирования платежа.
    3.  Данный метод не защищен арбитражем CryptoCraft.

#### 7.2.1. Альтернативные каналы платежей: Binance / Bybit / Telegram (концепт)

- Цель: дать пользователям возможность совершать оплату в пользу казначейского кошелька площадки через сторонние каналы, когда TON недоступен или неудобен.
- Варианты:
  - Telegram: мини-приложение/бот с выдачей реквизитов и подтверждением транзакции (off-chain подтверждение + ручная/полуавтоматическая сверка).
  - Binance / Bybit: при условии соответствия ToS — формирование инструкции/ссылки на перевод на корпоративный аккаунт (без хранения чувствительных данных на нашей стороне).
- Риски и комплаенс:
  - Проверить правила бирж по приёму средств на корп.аккаунт (B2C/маркетплейс сценарий), ограничения по регионам, KYC/AML.
  - Предупреждать пользователя о рисках и отсутствии арбитража для перевода вне TON-эскроу.
- UI/UX:
  - На шаге оплаты — «Метод оплаты»: TON эскроу (по умолчанию), Binance, Bybit, Telegram.
  - Пошаговые инструкции, квитанция подтверждения, статус «Ожидание подтверждения» с уведомлениями.

### 7.3. Аутентификация товаров и NFT-сертификаты

Этот процесс предназначен для повышения доверия к дорогим товарам.
1.  **Запрос:** Покупатель при оформлении заказа может выбрать опцию "Заказать проверку подлинности".
2.  **Отправка эксперту:** Статус заказа меняется на `SHIPPED_TO_EXPERT`. Продавец отправляет товар не покупателю, а в экспертный центр CryptoCraft.
3.  **Проверка:** Эксперты проверяют товар. Статус меняется на `PENDING_AUTHENTICATION`.
4.  **Результат:**
    *   **Успех:** Статус меняется на `AUTHENTICATION_PASSED`. Генерируется отчет. Может быть выпущен NFT-сертификат (статус `NFT_ISSUED`). Товар отправляется покупателю.
    *   **Провал:** Статус `AUTHENTICATION_FAILED`. Товар возвращается продавцу, а средства — покупателю.

### 7.4. Система споров (Арбитраж)

1.  **Открытие спора:** Покупатель или продавец может инициировать спор по заказу. Статус заказа меняется на `DISPUTED`.
2.  **Обсуждение:** Создается специальная комната в `DisputeCenter`, где обе стороны и администратор могут общаться и предоставлять доказательства.
3.  **Решение:** Администратор принимает решение (`RESOLVED_BUYER` или `RESOLVED_SELLER`) и при необходимости производит финансовые корректировки.

### 7.5. Управление DAO

1.  **Создание предложения:** PRO-пользователь может создать `Proposal` (предложение) с описанием и сроком голосования.
2.  **Голосование:** Другие PRO-пользователи могут голосовать `FOR` (За) или `AGAINST` (Против).
3.  **Завершение:** По истечении срока (`endsAt`), предложение переходит в статус `PASSED` или `REJECTED` в зависимости от результатов.
4.  **Исполнение:** Администратор вручную исполняет принятые предложения и меняет их статус на `EXECUTED`.

---

### 7.6. PRO-статус по отзывам (без ручной верификации)

- Присвоение PRO: автоматически при достижении строго ≥20 отзывов с 5★.
- Отрицательные/1★ отзывы на присвоение не влияют.
- Продление PRO: минимум 1 отзыв с 5★ за каждый календарный месяц; при отсутствии — статус снимается до повторного выполнения условия.
- Защита от накруток: отзывы доступны только после подтверждённого получения (подтверждение доставки от почтового оператора/удачно завершённая сделка).

### 7.7. Режимы покупки: «через корзину» и «без корзины»

- По умолчанию все категории продаются через корзину, исключения: транспорт (авто/мото/авиа) и недвижимость (аренда/продажа) — «без корзины».
- Переключение режима «корзина/без корзины» задаётся в админ-панели на уровне категории (переключатель/галочка), с наследованием на подкатегории.
- Для «без корзины»: сделка при встрече с бронированием слота и внесением задатка в эскроу; если сделка не состоялась — задаток возвращается.

### 7.8. Платежи и эскроу (USDT-only)

- Блокчейн: TON как сеть; валюта расчётов — только USDT в сети TON (в избежание волатильности).
- Эскроу обязателен для покупок через корзину.
- Для «без корзины»: задаток блокируется в эскроу до завершения встречи; при отмене или несостоявшейся сделке — возврат задатка.
- Комиссия площадки задаётся в админ-панели (по умолчанию 2%, может изменяться).

### 7.9. Логистика и встречи

- Электроника и обычные товары — доставка (например, Нова Пошта и др.).
- Авто/недвижимость — оффлайн передача.
- Модуль встречи/осмотра: бронирование слотов, адрес/гео-точка, подтверждения/отмены, связка с задатком в эскроу.

### 7.10. Отзывы и репутация

- Оставлять отзыв могут только участники подтверждённой сделки/заказа.
- Разрешены фото/видео в отзывах.
- Возможен антифрод-анализ отзывов (вес отзывов, выявление шаблонов накрутки) на базе AI.

### 7.11. AI-автоматизация

- Генерация листингов (название/описание/атрибуты/цена) — да (DeepSeek).
- Автомодерация контента и фото — да (DeepSeek для анализа контента).
- Ассистент в чате (ответы про товар/встречи/цену) — да (DeepSeek).
- Антифрод/скоринг отзывов и активности — да (DeepSeek).

#### 7.11.1. Провайдеры AI: DeepSeek по умолчанию, Gemini — только для фоторедактора

- Текущий статус:
  - Все текстовые и мультимодальные задачи (генерация листинга, импорт/парсинг HTML, аналитика, модерация) выполняются через DeepSeek.
  - Редактирование изображения (image-to-image) выполняется через Gemini как специализированный инструмент.
- Причина разделения: стоимость и покрытие возможностей. DeepSeek — заметно дешевле на токенах, Gemini — удобен для качественного редактирования изображений.
- Конфигурация: выбор провайдера задаётся на бэкенде в `AiService` и в ENV (режимы text/vision/edit-image).

### 7.12. Категории и динамические поля

- Для каждой категории в админке задаются динамические атрибуты (например, VIN, техпаспорт, кадастровый номер) и флажки «обязательное поле».
- Поля наследуются подкатегориями, возможна переопределяемость.

### 7.13. Языки и цены, P2P в кошельке продавца

- Локали: RU/UA/EN (UI/контент локализуемы).
- Отображение цен: базовая валюта — USDT; показываем справочные фиатные эквиваленты по курсам P2P.
- В кошельке продавца — P2P-модуль для вывода: карточные переводы, наличные, переводы по почте (для UA), без горячего вывода криптоактива «со стороны».

### 7.14. Комплаенс

- KYC отсутствует (площадка — свободная зона цифровых транзакций). Соблюдаем базовую безопасность и антифрод.

### 7.15. Расширение возможностей продавца

- Бесплатная доставка: продавец может включить опцию free-shipping на товар/вариант/корзину (по правилам категории/локации).
- Гибкие купоны:
  - На конкретный товар (SKU/ID),
  - На любой один товар в корзине (применяет максимальную выгоду или по выбору),
  - На всю корзину (условия: мин. сумма, количество, новые/старые пользователи и т.д.).
- Конфликты скидок: приоритеты и совместимость задаются правилами (например, один купон на заказ; продавец может разрешить стек).

## ✨ Недавние улучшения

1.  **Очистка проекта:** Были удалены неиспользуемые и дублирующиеся файлы для улучшения структуры и читаемости кодовой базы. Удалены: `components/CartPage.tsx`, `components/CreateListingPage.tsx`, `components/EditListingPage.tsx`, `pages/LandingPage.tsx`, `components/ShowcaseHeader.tsx`, `components/ShowcaseFooter.tsx`.
2.  **Автономность админ-панели:** Проведен рефакторинг, который сделал админ-панель полностью независимой от основного frontend-приложения.
    *   **Проблема:** Ранее админ-панель импортировала типы (`types.ts`) из папки основного приложения, что делало невозможным ее сборку и развертывание на отдельном сервере.
    *   **Решение:** Файл с типами был синхронизирован и помещен внутрь папки `admin`. Все пути импорта в сервисах админ-панели были исправлены для использования локальной копии типов.
    *   **Результат:** Теперь админ-панель является полностью самодостаточным приложением, готовым к деплою на любой хостинг отдельно от основного сайта.
3.  **Полная переработка документации:** Этот файл `README.md` был полностью переписан для создания исчерпывающего и профессионального руководства по проекту.
4.  **Безопасная загрузка изображений:** Бэкенд выполняет валидацию форматов/размеров, автоматически ресайзит и перекодирует изображения (WebP ≤1920px, ≤8 МБ) перед загрузкой в Cloudinary. Ссылочные загрузки проходят ту же санацию, что упрощает модерацию и защиту контента.
5.  **Версии черновиков товаров:** каждое сохранение создаёт историю правок с привязкой к автору. Можно предварительно просматривать версию, загружать её в форму или полностью восстановить. Продавец также получил кнопки «Предпросмотр» текущего черновика.
6.  **Наблюдаемость уровня продакшн:** backend подключён к `nestjs-pino` с traceId, структурированными логами и новым `/metrics` (Prometheus + Grafana). Документ [docs/observability.md](../backend/docs/observability.md) описывает набор дашбордов и базовые алерты.
7.  **Оптимизация фронта:** маршрутный code-splitting, кеширование ответов API, мемоизация карточек и виртуализация мастерской позволяют загружать маркетплейс быстрее и снижать нагрузку на клиент.
8.  **Тестовый контур:** Jest/ts-jest возвращены в проект, добавлены покрытие для `SettingsService` и `CategoriesService`, теперь `npm run test` проверяет критичные бизнес-правила перед деплоем.

---

## 🗺️ Дорожная карта (Roadmap)

Эта карта — живой документ. Мы обновляем её по мере реализации и уточнения целей.

### Вехи (Milestones)

- M1: MVP-маркетплейс (каталог, карточка, корзина→заказ, чат, авторизация Telegram)
- M2: Продавец PRO (панель продавца, промокоды, аналитика, верификация)
- M3: AI-пакет (генерация листинга, импорт с сайтов, фоторедактор)
- M4: Безопасные платежи (TON, эскроу, статусы заказов, возвраты)
- M5: Аутентификация + NFT (экспертная проверка, сертификаты)
- M6: Сообщество (Workshop, Форум, Live-трансляции)
- M7: Админ-панель (модерация, финансы, категории, иконки, настройки)
- M8: Надёжность и безопасность (логирование, rate limits, миграции, мониторинг)

---

## ✅ Готово / 🔧 В работе / 🧩 Запланировано

### ✅ Готово
- Бэкенд на NestJS: модули `auth`, `users`, `products`, `orders`, `chats`, `ai`, `categories`, `disputes`, `governance`, `livestreams`, `notifications`, `collections`, `promocodes` (базовая структура и эндпоинты)
- JWT-аутентификация, Telegram WebApp вход (`/auth/telegram`, `/auth/telegram/web-login`)
- Frontend (покупатели/продавцы) и Admin — отдельные Vite-приложения
- Интеграция с LiveKit (клиент) и Cloudinary (загрузка)
- Глобальная валидация запросов (ValidationPipe) и CORS

— Категории и динамические атрибуты (база): есть, поддержка динамических полей в модельках/типах
— Админ: настройки (комиссия и пр.) — модуль есть, API частично

### 🔧 В работе
- Админ-панель: переход с моков на реальные эндпоинты (`backendApiService` частично реализован)
- Категории и динамические атрибуты: генерация/сохранение через AI (`/ai/generate-and-save-subcategories`)
- Локализация дат/валют и согласование типов на фронте/бэке (переезд на общие DTO)

— Политика PRO по отзывам и логика продления — проектирование
— Переключатель «корзина/без корзины» в админке на уровне категории — интерфейс/схема

### 🧩 Запланировано (предлагаемая первичная приоритезация)
- Безопасность продакшена:
  - Отключить `synchronize` в проде, перейти на миграции TypeORM
  - Жёстко требовать `JWT_SECRET`, `DATABASE_URL`, `TELEGRAM_BOT_TOKEN` при старте
  - Пересмотреть `ssl.rejectUnauthorized=false` в проде
  - Включить `@nestjs/throttler` для `/auth/admin/login`, добавить `helmet`
  - Перенос токена из `localStorage` в HttpOnly cookie (по возможности)
- Контракты:
  - Вынести общие DTO/типы в shared-пакет и использовать во всех приложениях
  - Нормализовать даты в ISO на API; форматирование — на клиенте
- Платежи (TON):
  - Реализовать USDT-only эскроу (корзина) и задаток в эскроу (без корзины)
  - Прописать статусы и жизненный цикл платежа/эскроу, webhooks/поллинг
  - Фин. отчётность: комиссии, выплаты, рефанды
- Аутентификация товаров + NFT:
  - Прописать бизнес-процесс, роли, статусы, артефакты (отчёт, сертификат)
- Наблюдаемость:
  - Структурированные логи, централизованный сбор, алёрты (уровни ERROR/SECURITY)
  - Трассировка запросов и метрики (Prometheus/OpenTelemetry)
- Производительность и DX:
  - Разделение бандла, мемоизация, виртуализация списков, кэширование API
  - E2E/интеграционные тесты критичных сценариев

- Встречи/осмотры: бронирование слотов, адрес/гео, связи с задатком, отмены
- Отзывы: подтверждение доставки, медиа в отзывах, антифрод на AI
- Локализация RU/UA/EN, цены в USDT + фиатная справка по P2P
- P2P-модуль в кошельке продавца (вывод на карту/наличные/почтой)

---

## 🛡️ Безопасность и соответствие

- Продакшен:
  - TypeORM `synchronize` — выключить; использовать миграции
  - Требовать `JWT_SECRET`, `DATABASE_URL`, `TELEGRAM_BOT_TOKEN` → fail-fast
  - CORS — источники только из ENV, без жёсткого списка в коде
  - SSL — избегать `rejectUnauthorized=false` на бою
- Аутентификация/авторизация:
  - Admin: убрать дефолты `admin/admin`, включить rate-limit, логи входа
  - Пользовательские токены — рассмотреть HttpOnly cookie
- Данные и конфиденциальность:
  - Хранить минимально необходимое, аудировать доступ к PII

---

## 🤝 Вклад и правила PR

1. Создайте feature-ветку от `main` (`feat/<scope>-<short-name>`)
2. Пишите понятные коммиты (`conventional commits` приветствуются)
3. Покрывайте критичные кейсы тестами (юнит/интеграция)
4. В PR указывайте:
   - Что сделано и почему
   - Скриншоты UI/видео (если меняется интерфейс)
   - Риски/миграции/ENV

---

## 📚 Журнал решений (ADR-lite)

- 2025-11-04 — Backend как модульный монолит (скорость разработки, упрощение деплоя)
- 2025-11-04 — Два отдельных фронтенда (marketplace/admin) для независимого релиза
- 2025-11-04 — AI-проксирование только через backend (скрытие ключей)

---

## ❓ FAQ по разработке

**Q:** Где менять CORS?  
**A:** В `backend/src/main.ts` + ENV `FRONTEND_URL`, `ADMIN_URL`.

**Q:** Как подключить базы и миграции?  
**A:** Настроить `DATABASE_URL`, отключить `synchronize`, добавить скрипты миграций TypeORM.

**Q:** Где находятся клиенты API на фронте?  
**A:** `src/services/apiService.ts` (marketplace), `admin/services/backendApiService.ts` (admin).

—

> Следующий шаг: вы даёте мне ваши цели/этапы — я уточняю и вношу в раздел Roadmap (вехи и чек-листы) с приоритетами и критериями готовности.
