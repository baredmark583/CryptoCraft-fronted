# CryptoCraft Marketplace - Техническая документация

## 1. Введение

**CryptoCraft** — это полнофункциональный маркетплейс в стиле Etsy, спроектированный для продажи уникальных товаров с особым акцентом на изделия ручной работы, электронику и коллекционные предметы. Платформа предлагает современный, децентрализованный опыт электронной коммерции, объединяя мощные AI-инструменты для продавцов, интеграцию криптовалютных платежей через блокчейн TON и комплексную систему управления для администраторов.

Эта документация предоставляет исчерпывающий обзор архитектуры и функциональности трех ключевых компонентов проекта:
1.  **Клиентское приложение (Frontend):** Основной интерфейс для покупателей и продавцов.
2.  **Административная панель (Admin Panel):** Центр управления и модерации платформы.
3.  **Серверное приложение (Backend):** Ядро бизнес-логики, построенное на NestJS.

## 2. Технологический стек

### Фронтенд (Клиент)
-   **Фреймворк:** React 19 (с использованием Hooks)
-   **Язык:** TypeScript
-   **Стилизация:** TailwindCSS
-   **Роутинг:** React Router v6
-   **Крипто-платежи:** TON Connect UI React (`@tonconnect/ui-react`)
-   **Сборка:** Vite

### Админ-панель
-   **Фреймворк:** React 18
-   **Язык:** TypeScript
-   **Стилизация:** TailwindCSS
-   **Роутинг:** React Router v6
-   **Визуализация данных:** Recharts
-   **Сборка:** Vite

### Бэкенд (Сервер)
-   **Фреймворк:** NestJS
-   **Язык:** TypeScript
-   **База данных:** PostgreSQL
-   **ORM:** TypeORM
-   **AI-интеграция:** Google Gemini API (`@google/genai`)
-   **Аутентификация:** JWT, Passport.js
-   **Хранение медиа:** Cloudinary
-   **Веб-скрапинг:** Axios, Cheerio

---

## 3. Клиентское приложение (Frontend)

Основное приложение, с которым взаимодействуют пользователи. Оно построено на принципах современного, отзывчивого и интуитивно понятного UI/UX.

### 🤖 3.1. AI-Инструменты: Ваш умный бизнес-партнер

Ключевое преимущество CryptoCraft — глубокая интеграция AI для автоматизации и упрощения работы продавцов. Все AI-запросы безопасно проксируются через бэкенд (`backend/src/ai/ai.service.ts`), который управляет API-ключами и логикой взаимодействия с Gemini.

#### 3.1.1. AI-генерация и пакетная загрузка объявлений
-   **Функциональность:** Продавец загружает одно фото товара и пишет краткое описание. AI анализирует эти данные и автоматически генерирует полное, готовое к публикации объявление: SEO-оптимизированный заголовок, подробное описание, рыночную цену, категорию и релевантные атрибуты.
-   **Реализация:**
    -   **UI:** Страница `CreateListingPage.tsx` содержит компонент `AIGenerateForm`, который отправляет изображение (в формате Base64) и текст на бэкенд.
    -   **Логика:** `geminiService.ts` вызывает `apiService.generateListingWithAi`, который обращается к эндпоинту `/ai/generate-listing`.
    -   **Backend:** `ai.service.ts` формирует сложный промпт для модели `gemini-2.5-flash`, передавая ей изображение, текст и СТРОГУЮ JSON-схему для ответа. Это гарантирует получение структурированных данных.
    -   **Пакетная работа:** Сгенерированные объявления не публикуются сразу, а добавляются в "пакет" на той же странице (`ListingReviewCard`). Продавец может отредактировать их и опубликовать все вместе одним кликом, что идеально для массовой загрузки товаров.

#### 3.1.2. AI "Фотостудия"
-   **Функциональность:** Интегрированный редактор для превращения обычных фотографий в студийные. Пользователь может удалить фон, улучшить освещение, добавить тени или поместить товар в совершенно другое окружение с помощью текстовых промптов.
-   **Модель Freemium:** Standard-пользователи получают 5 бесплатных правок в день, Pro — 15. Каждая последующая правка стоит 0.36 USDT, которые списываются с внутреннего баланса.
-   **Реализация:**
    -   **UI:** Страница `PhotoStudioPage.tsx` позволяет выбрать изображение товара, ввести промпт и мгновенно увидеть результат.
    -   **Логика:** `geminiService.editImage` отправляет Base64-изображение, его MIME-тип и текстовый промпт на бэкенд. Логика подсчета правок и списания средств (`EDIT_COST`) реализована на клиенте с использованием `localStorage` и `apiService.updateUserBalance`.
    -   **Backend:** `ai.service.ts` использует мультимодальную модель `gemini-2.5-flash-image-preview` для редактирования изображений, возвращая новую картинку в формате Base64.

#### 3.1.3. Интеллектуальный поиск
-   **Функциональность:** Поисковая система понимает естественный язык. Запрос "красивая синяя ваза из глины" превращается в структурированные фильтры: `{ "keywords": ["ваза", "глина", "синяя"], "category": "Товары ручной работы" }`, делая поиск точнее.
-   **Реализация:**
    -   **UI:** `SearchResultsPage.tsx` при получении поискового запроса сначала обращается к AI.
    -   **Логика:** `geminiService.generateSearchQuery` (на данный момент мок на фронтенде) преобразует пользовательский запрос в объект `StructuredSearchQuery`. Эти данные затем используются для фильтрации товаров через `apiService.getProducts`.

#### 3.1.4. Верификация Pro-статуса
-   **Функциональность:** Двухуровневая система доверия (Standard и Pro). Пользователи могут стать "Pro-продавцом", пройдя верификацию документов.
-   **Реализация:**
    -   **UI:** На странице `VerificationPage.tsx` пользователь загружает фото документа. `CameraCaptureModal.tsx` позволяет сделать снимок напрямую с устройства.
    -   **Логика:** `geminiService.analyzeDocumentForVerification` отправляет изображение на бэкенд.
    -   **Backend:** `ai.service.ts` использует `gemini-2.5-flash` для анализа изображения, определяя, является ли оно документом, и пытаясь извлечь ФИО. Это симулирует процесс проверки для присвоения статуса.

### 🛒 3.2. E-commerce: Надежный фундамент для торговли

#### 3.2.1. Умная Система Промокодов
-   **Функциональность:** Мощный маркетинговый инструмент, позволяющий продавцам создавать гибкие промо-кампании (скидка в % или сумме, применение к категориям, условия по сумме заказа).
-   **Реализация:**
    -   **UI:** Вкладка "Настройки" (`SettingsTab.tsx`) в профиле продавца содержит интерфейс для создания, просмотра и удаления промокодов.
    -   **AI-помощник:** Продавец может описать цель акции (например, "распродать летнюю коллекцию"), и `geminiService.generatePromoCampaign` (мок) предложит готовую конфигурацию промокода.
    -   **Применение:** В `CheckoutPage.tsx` покупатели могут применить промокод, который валидируется через `apiService.validatePromoCode`.

#### 3.2.2. Разделение заказов
-   **Функциональность:** Корзина (`CartPage.tsx`) автоматически группирует товары по продавцам. При оформлении заказа (`CheckoutPage.tsx`) система создает отдельный заказ для каждого продавца.
-   **Реализация:**
    -   **Frontend:** `useCart` и `CheckoutPage` подготавливают сгруппированные данные.
    -   **Backend:** `orders.service.ts` в методе `create` итерируется по группам товаров, создавая несколько сущностей `Order` в рамках одной транзакции базы данных, что гарантирует атомарность операции.

#### 3.2.3. Сервис Аутентификации и NFT-сертификаты
-   **Функциональность:** Система для проверки подлинности дорогих товаров (например, электроники, автомобилей). После проверки экспертом выпускается уникальный **NFT-сертификат** в сети TON, подтверждающий подлинность.
-   **Реализация:**
    -   **UI:** `AuthenticationCenterPage.tsx` позволяет отслеживать все этапы проверки. `NFTCertificateModal.tsx` красиво отображает выданный сертификат. Продавец может запросить проверку через `AuthenticationRequestModal.tsx`.
    -   **Логика:** Статусы заказа (`OrderStatus` в `types.ts`) меняются по мере прохождения этапов. Данные NFT (адрес контракта, ID токена) хранятся в сущности `Product`.

#### 3.2.4. Контекстный чат
-   **Функциональность:** Чат — это инструмент продаж. При переходе в чат с карточки товара (`ProductDetailPage.tsx`), в диалог автоматически добавляется карточка этого товара и предлагаются быстрые ответы, релевантные его категории.
-   **Реализация:**
    -   **UI:** `ChatPage.tsx` проверяет `productId` в URL. Если он есть, загружает товар и отправляет системное сообщение с ним.
    -   **Логика:** На основе категории товара генерируются релевантные `quickReplies` (например, "Есть ли гарантия?" для электроники), ускоряя коммуникацию.

#### 3.2.5. Гибкая Система Вариантов Товара
-   **Функциональность:** Позволяет продавать один товар в разных вариациях (например, футболка разных размеров и цветов). Для каждой комбинации можно указать **индивидуальную цену, количество на складе и отдельное изображение**.
-   **Реализация:**
    -   **UI:** В `EditListingPage.tsx` находится компонент `VariantEditor`, где продавец определяет атрибуты (Цвет, Размер) и их опции (Красный, Синий; S, M, L).
    -   **Логика:** Компонент автоматически генерирует все возможные комбинации вариантов. Продавец заполняет для каждой цену и остаток. Данные хранятся в `product.variants` и `product.variantAttributes`. `ProductDetailPage` отображает селекторы для выбора варианта.

### 🧑‍💼 3.3. Центр Управления Продавца (`ProfilePage.tsx`)

Личный кабинет — это полноценный дашборд для управления бизнесом.

-   **Контекстная Сводка:** Вкладка "Сводка" (`DashboardTab.tsx`) адаптируется под продавца. Если большинство его товаров — электроника, отображается специализированный дашборд `ElectronicsDashboardTab.tsx` с метриками по аутентификации.
-   **Проактивные Продажи:** `DashboardTab.tsx` отображает ленту активности, где продавец видит, кто добавил его товар в избранное. Кнопка "Предложить скидку" открывает `OfferModal.tsx`, позволяя отправить пользователю персональное предложение с существующим промокодом.
-   **AI-Аналитик:** Вкладка "Аналитика" (`AnalyticsDashboard.tsx`) содержит компонент `AiAssistant`, который отправляет данные о продажах на бэкенд и получает от AI конкретные советы по увеличению продаж.
-   **Интеллектуальный Импорт:** Страница `ImportPage.tsx` позволяет автоматически перенести магазин с другого маркетплейса. Продавец вставляет ссылки, а бэкенд запускает сложный процесс:
    1.  `scraping.service.ts` через прокси получает HTML-код страницы.
    2.  `ai.service.ts` (`processImportedHtml`) анализирует "сырой" HTML, извлекая всю информацию о товаре и преобразуя ее в структурированный JSON.
    3.  `upload.service.ts` скачивает изображения по найденным URL и загружает их в Cloudinary.
    4.  Готовые черновики отображаются на фронтенде для финальной проверки и публикации.

### 🤝 3.4. Социальные и Коммерческие Экосистемы

-   **Центр Сообщества и Live-commerce:** `CommunityHubPage.tsx` объединяет форум для обсуждений и секцию прямых эфиров. Pro-продавцы могут создавать трансляции на `CreateLiveStreamPage.tsx` и проводить их на `LiveStreamPage.tsx`.
-   **Центр Разрешения Споров:** `DisputeCenterPage.tsx` предоставляет интерфейс для арбитража между покупателем и продавцом, где они могут обмениваться сообщениями и доказательствами под наблюдением администратора.
-   **DAO (Децентрализованное управление):** `GovernancePage.tsx` позволяет Pro-продавцам голосовать по предложениям, влияющим на развитие платформы (например, изменение комиссии). Детали каждого предложения — на `ProposalDetailPage.tsx`.

---

## 4. Административная панель (Admin Panel)

Отдельное React-приложение для полного управления, модерации и анализа маркетплейса.

### 4.1. Архитектура
-   **Технологии:** React, TypeScript, TailwindCSS, React Router.
-   **API:** Взаимодействует с бэкендом NestJS через защищенный набор эндпоинтов. `admin/services/backendApiService.ts` инкапсулирует все вызовы к API.
-   **Аутентификация:** Вход по логину/паролю, которые хранятся в `.env` на бэкенде.

### 4.2. Ключевые Модули и Функциональность

#### 4.2.1. Главная Панель (Dashboard)
-   **Реализация:** `admin/pages/DashboardPage.tsx`
-   **Функциональность:** Мощный аналитический центр, отображающий ключевые метрики (KPI) в реальном времени: общий оборот, прибыль платформы, количество новых заказов, товары на модерации и активные споры. Использует `Recharts` для визуализации динамики продаж (`SalesChart.tsx`).

#### 4.2.2. Управление Пользователями
-   **Реализация:** `admin/pages/UsersPage.tsx`, `UserDetailPage.tsx`
-   **Функциональность:** Полный CRUD-интерфейс для всех пользователей. Администратор может:
    -   Искать и фильтровать пользователей.
    -   Просматривать детальную информацию: все товары, продажи, покупки, споры.
    -   Редактировать данные пользователя (`EditUserModal.tsx`), включая баланс и Pro-статус.
    -   Блокировать и разблокировать пользователей.

#### 4.2.3. Управление Контентом и Товарами
-   **Модерация Товаров:** `admin/pages/ProductModerationPage.tsx` представляет собой очередь объявлений со статусом `Pending Moderation`. В `ProductModerationModal.tsx` модератор может просмотреть всю информацию о товаре и одобрить его или отклонить с указанием причины.
-   **Управление Категориями:** `admin/pages/CategoriesPage.tsx` — это мощный инструмент, который полностью заменяет константы в коде. Администратор может динамически создавать, редактировать и удалять категории и их атрибуты (поля).
    -   **AI-генерация:** Для любой категории можно запустить AI-генерацию подкатегорий. Бэкенд (`ai.service.ts`) создает полную, логичную структуру с релевантными полями и автоматически сохраняет ее в базу данных.
-   **Библиотека Иконок:** `admin/pages/IconsPage.tsx` позволяет централизованно управлять всеми системными иконками, используемыми на фронтенде. Администратор может заменить любую иконку, вставив SVG-код или URL с Iconify, а также задать для нее индивидуальные размеры. Это обеспечивает гибкую кастомизацию UI без изменения кода.

#### 4.2.4. Заказы и Споры
-   **Просмотр заказов:** `admin/pages/OrdersPage.tsx` предоставляет интерфейс для просмотра и управления всеми заказами в системе.
-   **Центр Разрешения Споров:** `admin/pages/DisputesPage.tsx` — выделенный интерфейс для арбитража. Администратор видит всю переписку сторон в `DisputeDetailsModal.tsx` и выносит решение, которое завершает спор.

#### 4.2.5. Финансы и Маркетинг
-   **Просмотр Транзакций:** `admin/pages/FinancesPage.tsx` (вкладка "Транзакции") отображает лог всех финансовых операций в системе.
-   **Промокоды:** Вкладка "Промокоды" позволяет создавать "общие" промокоды от имени платформы, которые могут использовать все пользователи.

#### 4.2.6. Управление платформой
-   **Реализация:** `admin/pages/SettingsPage.tsx`
-   **Функциональность:** Центральный пульт управления платформой, разделенный на логические вкладки:
    -   **Общие:** Включение/выключение **режима технического обслуживания** сайта для пользователей.
    -   **Финансы:** Гибкая настройка **комиссий** (базовая, для Pro-продавцов, минимальная сумма), управление кошельком для безопасных сделок.
    -   **Интеграции:** Управление API-ключами для внешних сервисов (Google Gemini, Cloudinary) с функцией **проверки работоспособности** каждого ключа.

#### 4.2.7. Отладка
-   **Debug-меню:** `admin/pages/DebugPage.tsx` реализует встроенную консоль, которая в реальном времени отображает логи с бэкенда (симуляция через `adminApiService`, но рассчитана на WebSocket). Это позволяет отслеживать состояние сервера без прямого доступа к терминалу.

---

## 5. Архитектура Бэкенда (Backend)

Сердце проекта, разработанное на NestJS, обеспечивает надежную и масштабируемую основу для всех операций.

### 5.1. Ключевые Модули

-   **`AuthModule`:** Управляет аутентификацией. `auth.service.ts` содержит ключевую логику:
    -   `validateTelegramData`: Валидирует данные от Telegram Web App, проверяя криптографическую подпись с помощью `TELEGRAM_BOT_TOKEN`.
    -   `validateAdmin`: Проверяет логин/пароль администратора из переменных окружения.
    -   `login`: Генерирует JWT для аутентифицированных пользователей.

-   **`UsersModule`:** Отвечает за CRUD операции с пользователями. `users.service.ts` содержит метод `findByTelegramIdOrCreate`, который автоматически регистрирует новых пользователей при первом входе через Telegram.

-   **`ProductsModule`:** Управляет жизненным циклом товаров, включая создание, обновление и модерацию статусов (`Pending Moderation`, `Active`, `Rejected`).

-   **`OrdersModule`:** Содержит сложную бизнес-логику для обработки заказов. Метод `create` в `orders.service.ts` выполняет критически важные операции в рамках транзакции TypeORM:
    1.  Группирует товары по продавцам.
    2.  Для каждой группы проверяет наличие достаточного количества товара на складе (stock).
    3.  Создает отдельные сущности `Order` для каждого продавца.
    4.  Уменьшает количество товара на складе.
    5.  В случае ошибки на любом шаге вся транзакция откатывается.

-   **`AiModule`:** Является безопасным прокси для Google Gemini API. `ai.service.ts` инкапсулирует все API-ключи, промпты и JSON-схемы. Это позволяет фронтенду запрашивать сложные AI-операции, не имея прямого доступа к API, и гарантирует получение данных в нужном формате.

-   **`UploadModule`:** Обрабатывает загрузку файлов. `upload.service.ts` принимает файлы от клиента, стримит их в Cloudinary и возвращает безопасный URL. Также реализована функция `uploadFileFromUrl` для импорта изображений с других сайтов.

-   **`ScrapingModule`:** `scraping.service.ts` использует `axios` и `cheerio` для извлечения HTML-содержимого по URL. Для обхода базовых защит используется публичный прокси `allorigins.win`. Сервис очищает HTML от ненужных тегов и атрибутов перед отправкой в `AiModule`.

-   **`DashboardModule`:** Агрегирует данные для админ-панели. `dashboard.service.ts` выполняет сложные запросы к нескольким таблицам (Orders, Users, Products), чтобы вычислить KPI, построить данные для графиков и собрать ленту активности.

-   **`CategoriesModule`, `IconsModule`, `SettingsModule`:** Набор модулей, которые делают платформу полностью управляемой через админ-панель. Они предоставляют CRUD-эндпоинты для управления категориями, системными иконками и глобальными настройками, хранящимися в базе данных.
