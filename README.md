# CryptoCraft Marketplace - Техническая документация

Это фронтенд-часть проекта **CryptoCraft**, маркетплейса в стиле Etsy для уникальных товаров ручной работы. Платформа предлагает современный и децентрализованный опыт коммерции с упором на AI-инструменты для продавцов и интеграцию криптовалютных платежей.

Весь бэкенд симулируется с помощью mock-сервиса (`src/services/apiService.ts`), что позволяет демонстрировать полную функциональность приложения без необходимости развертывания реального сервера.

## Технологический стек

-   **Фреймворк:** React
-   **Язык:** TypeScript
-   **Стилизация:** TailwindCSS
-   **Роутинг:** React Router
-   **AI-интеграция:** Google Gemini API (`@google/genai`)
-   **Крипто-интеграция (Mock):** Симуляция подключения кошелька TON
-   **Хранение медиа (Mock):** Cloudinary (`src/services/cloudinaryService.ts`) для симуляции загрузки, хранения и оптимизации изображений и видео.

---

## Реализованная функциональность (Подробный обзор)

Этот раздел подробно описывает ключевые механики, уже реализованные в приложении. Каждая функция объясняется с точки зрения ее ценности для пользователя и принципа работы.

### 🤖 AI-Инструменты: Ваш умный бизнес-партнер

Ядро платформы — это мощные AI-инструменты, которые кардинально упрощают и автоматизируют работу продавцов.

#### 1. Интеллектуальная AI-генерация объявлений
-   **Что это?** Волшебный помощник, который создает профессиональное объявление на основе всего одной фотографии и короткой фразы.
-   **Как это работает?** В основе лежит гибкая архитектура **динамических схем категорий** (`constants.ts`). Каждая категория товаров (например, "Электроника" или "Автомобили") имеет свой набор уникальных характеристик. Когда продавец загружает фото и пишет, например, "Продам Audi A6 2019 года", AI (`gemini-2.5-flash` на `CreateListingPage.tsx`) выполняет три шага:
    1.  **Классификация:** Определяет, что это "Автомобили".
    2.  **Извлечение атрибутов:** Находит и извлекает из текста и фото данные, соответствующие схеме этой категории (модель, год, пробег и т.д.).
    3.  **Генерация контента:** Пишет привлекательный заголовок, подробное описание и предлагает рыночную цену.
-   **В чем ценность?** Экономит продавцам часы времени, помогает создавать качественные описания и делает платформу **бесконечно масштабируемой** — для добавления нового типа товаров достаточно описать его характеристики, не меняя код.

#### 2. AI "Фотостудия"
-   **Что это?** Встроенный AI-редактор (`PhotoStudioPage.tsx`) для превращения обычных фотографий в профессиональные студийные изображения. Теперь эта функция работает по модели **freemium** для обеспечения устойчивости платформы.
-   **Как это работает?** Продавец может выбрать быстрое действие ("Удалить фон") или написать свой запрос ("поставь эту чашку на мраморный стол"). Модель `gemini-2.5-flash-image-preview` получает **только титульное (главное) изображение товара** и текстовый запрос, а возвращает уже отредактированную версию.
-   **Монетизация и лимиты:**
    -   **Standard-пользователи:** Получают **5 бесплатных** редактирований в сутки.
    -   **Pro-продавцы:** Получают **15 бесплатных** редактирований в сутки.
    -   **Стоимость сверх лимита:** Каждое последующее редактирование стоит **0.36 USDT**, которые списываются с внутреннего баланса пользователя.
-   **В чем ценность?** Позволяет продавцам без навыков дизайнера создавать высококачественный визуальный контент, что напрямую влияет на продажи. Условно-платная модель делает мощный AI-инструмент доступным для всех, одновременно покрывая операционные расходы и мотивируя серьезных продавцов переходить на Pro-статус.
-   **Заметка для бэкенда:** Необходимо реализовать следующую логику:
    1.  **Хранение лимитов:** В модели `User` добавить поле, отслеживающее количество использованных редактирований за текущие сутки (например, `photoStudioUsage: { count: number, resetDate: timestamp }`).
    2.  **Проверка и списание:** Перед каждым запросом к Gemini API бэкенд должен проверять лимит. Если он исчерпан, проверять баланс и списывать 0.36 USDT. Если средств недостаточно, возвращать ошибку.

#### 3. Интеллектуальный поиск
-   **Что это?** Поисковая система, которая понимает человеческий язык.
-   **Как это работает?** Когда пользователь вводит "красивая синяя ваза из глины", AI (`gemini-2.5-flash` на `SearchResultsPage.tsx`) анализирует запрос и преобразует его в структурированные фильтры: `{ "keywords": ["ваза", "глина", "синяя"], "category": "Товары ручной работы" }`.
-   **В чем ценность?** Поиск становится значительно точнее и релевантнее, помогая покупателям быстрее находить именно то, что они ищут.

#### 4. Верификация Pro-статуса и двухуровневая система доверия
-   **Что это?** Простая и понятная система для повышения статуса продавца, которая напрямую влияет на доверие покупателей. Существует два типа пользователей: **Standard** (обычный) и **Pro** (проверенный).
-   **Как это работает?** Любой пользователь может повысить свой статус до **"Pro-продавца"**, пройдя одну простую процедуру верификации на странице `VerificationPage.tsx`. Пользователь загружает фото документа, и после успешной проверки ему присваивается Pro-статус, открывающий доступ к DAO и Live-commerce.

> **Архитектурное замечание: Реальный KYC через Sumsub**
>
> В текущей реализации процесс верификации документов является **симуляцией** для демонстрации концепции.
>
> В **реальном продакшн-приложении** для обработки документов и KYC (Know Your Customer) будет использоваться специализированный, сертифицированный провайдер, такой как **Sumsub**. Это гарантирует максимальную безопасность, соответствие законодательству (GDPR) и надежное распознавание документов из разных стран.

-   **В чем ценность?** Создает четкое различие между обычными пользователями и проверенными профессиональными продавцами. Pro-статус значительно повышает доверие покупателей, а также открывает доступ к мощным инструментам для серьезных продавцов, создавая премиальный уровень на платформе.

### 🛒 E-commerce: Надежный фундамент для торговли

#### 1. Умная Система Промокодов (Smart Promo Codes)
-   **Что это?** Мощный маркетинговый инструмент, который позволяет продавцам создавать гибкие и таргетированные промо-кампании, а не просто скидочные коды.
-   **Как это работает?** В настройках (`SettingsTab.tsx`) продавец может создать промоакцию с множеством параметров:
    *   **Гибкость и Таргетинг:**
        *   **Тип скидки:** Процент (%) или фиксированная сумма (USDT).
        *   **Область применения:** На весь магазин, на конкретную категорию или на выбранные товары.
        *   **Условия:** Например, "скидка действует только при покупке на сумму от 50 USDT".
    *   **Умные Ограничения:** Можно установить срок действия промокода и ограничить количество его использований.
    *   **AI-Помощник (Gemini Integration):** Вместо ручного заполнения формы, продавец может просто написать свою цель в текстовом поле, например: *"Хочу устроить распродажу на старую коллекцию керамики на этих выходных"*. AI проанализирует запрос и **автоматически предложит готовую конфигурацию промокода** (название, размер скидки, срок действия и т.д.), которую останется только подтвердить.
-   **В чем ценность?** Превращает промокоды из простого инструмента в полноценную систему управления маркетинговыми кампаниями. Это позволяет продавцам стимулировать продажи, увеличивать средний чек и проводить целевые акции с минимальными усилиями.

#### 2. Умная корзина и разделение заказов
-   **Что это?** Логика, которая корректно обрабатывает покупки у нескольких продавцов одновременно.
-   **Как это работает?** В корзине (`CartPage.tsx`) товары автоматически группируются по продавцам. При оформлении (`CheckoutPage.tsx`) система создает **отдельный заказ для каждого продавца**.
-   **В чем ценность?** Обеспечивает корректный и прозрачный процесс покупки, даже если в корзине товары от десятка разных мастеров.

#### 3. Анализ рынка через проданные товары
-   **Что это?** Инструмент для продавцов, позволяющий изучать спрос и тренды на основе уже проданных товаров.
-   **Как это работает?** В фильтрах каталога есть опция "Показать проданные". При ее активации на карточках товаров (`ProductCard.tsx`) появляется плашка "ПРОДАНО", а цена продажи остается видимой для анализа.
-   **Примечание:** В текущей mock-реализации `apiService.ts` эта функция симулируется. В реальной системе поиск будет осуществляться по базе данных завершенных заказов, а не по списку активных товаров.

#### 4. Сервис Аутентификации и NFT-сертификаты
-   **Что это?** Комплексная система для проверки подлинности дорогостоящих товаров (например, электроники или автомобилей), подкрепленная уникальным цифровым сертификатом на блокчейне.
-   **Как это работает?**
    1.  Продавец может заказать услугу проверки для своего товара.
    2.  Покупатель видит статус проверки на странице товара и может отслеживать весь процесс в "Центре Аутентификации" (`AuthenticationCenterPage.tsx`).
    3.  После успешной проверки выпускается **NFT-сертификат**, который можно просмотреть в стильном модальном окне (`NFTCertificateModal.tsx`). Этот сертификат является цифровым доказательством подлинности и происхождения товара.
-   **В чем ценность?** Резко повышает доверие к дорогим покупкам, решает проблему контрафакта и знакомит пользователей с преимуществами технологии блокчейн.

#### 5. Контекстный и интерактивный чат
-   **Что это?** Система чатов (`ChatPage.tsx`), превращенная в мощный инструмент для продаж.
-   **Как это работает?** При нажатии "Написать продавцу" чат открывается с карточкой товара. Покупатель видит кнопки с **быстрыми ответами, релевантными категории** ("Когда можно посмотреть?" для авто, "Есть ли гарантия?" для электроники). Также можно обмениваться изображениями.
-   **В чем ценность?** Упрощает коммуникацию, ускоряет процесс принятия решения о покупке и повышает конверсию.

#### 6. "Гонка за последний товар" (Защита от оверселлинга)
-   **Что это?** Механизм, который предотвращает ситуацию, когда два покупателя одновременно пытаются купить последний экземпляр товара.
-   **Как это работает?** В момент перехода к оформлению заказа (`CheckoutPage.tsx`) система на короткое время "резервирует" товары в корзине (`apiService.reserveProductsForCheckout`). Первый, кто успел, завершает покупку; остальные получают уведомление.
-   **В чем ценность?** Критически важная функция для любого маркетплейса, которая предотвращает негативный опыт и финансовые ошибки.

#### 7. Усовершенствованный интерфейс каталога
-   **Что это?** Ряд улучшений на странице списка товаров (`ProductListPage.tsx`), направленных на повышение удобства и визуальной целостности.
-   **Как это работает?**
    -   **Корректное наложение элементов:** Устранена проблема, при которой "липкая" боковая панель фильтров прокручивалась поверх шапки сайта. Путем тонкой настройки `z-index` обеспечена правильная иерархия слоев.
    -   **Масштабируемые фильтры:** Список категорий в боковой панели был преобразован в сворачиваемый аккордеон (`FilterBar.tsx`). Это решение предотвращает "растягивание" панели при большом количестве категорий, делая интерфейс аккуратным и готовым к росту ассортимента. Для длинных списков добавлена вертикальная прокрутка.
-   **В чем ценность?** Эти, казалось бы, небольшие изменения значительно улучшают пользовательский опыт, делая навигацию по каталогу более плавной и интуитивно понятной. Они демонстрируют внимание к деталям и готовность платформы к масштабированию контента без ущерба для юзабилити.

#### 8. Гибкая Система Вариантов Товара (Product Variants)
-   **Что это?** Мощный функционал, позволяющий продавать один товар в различных вариациях (например, футболка в разных размерах и цветах) без необходимости создавать десятки отдельных объявлений.
-   **Как это работает?**
    -   **Управление в `EditListingPage.tsx`:** Продавец активирует режим "вариантов" и определяет атрибуты (например, "Цвет", "Размер"). Для каждого атрибута он задает возможные опции ("Красный", "Синий"; "S", "M", "L").
    -   **Авто-генерация комбинаций:** Система автоматически создает все возможные комбинации этих опций.
    -   **Уникальные параметры:** Для каждой сгенерированной комбинации (варианта) продавец может указать **индивидуальную цену, количество на складе, артикул (SKU) и даже привязать отдельное изображение**.
-   **Пользовательский опыт в `ProductDetailPage.tsx`:** Покупатель видит удобные переключатели для выбора опций. При выборе варианта, **цена, статус наличия и главное изображение товара динамически обновляются**, обеспечивая интуитивно понятный и прозрачный процесс покупки.
-   **В чем ценность?** Это фундаментальная функция для любого серьезного маркетплейса, абсолютно необходимая для продавцов одежды, ювелирных изделий, принтов и многих других категорий товаров. Она делает управление ассортиментом простым и эффективным.

### 🧑‍💼 Центр Управления Продавца: Все под рукой

Личный кабинет (`ProfilePage.tsx`) — это полноценный дашборд для управления бизнесом.

-   **Контекстная Сводка:** Главная страница адаптируется под продавца. Если он торгует электроникой, ему показывается специализированный дашборд (`ElectronicsDashboardTab.tsx`) с релевантными метриками. Для всех остальных — стандартная сводка (`DashboardTab.tsx`).
-   **Проактивные Продажи:** На сводке продавец видит, кто добавил его товар в избранное, и может тут же отправить этому пользователю **персональное предложение со скидкой** (`OfferModal.tsx`), превращая интерес в продажу.
-   **Глубокая Аналитика с AI-советником:** Вкладка "Аналитика" (`AnalyticsDashboard.tsx`) не просто показывает графики, а предлагает **AI-Аналитика**, который изучает данные и дает конкретные советы по увеличению продаж.
-   **Финансовый Хаб ("Кошелек"):** Вкладка `WalletTab.tsx` позволяет отслеживать баланс, ожидаемые поступления, задолженность по комиссии и историю операций.
-   **Маркетинговые инструменты ("Настройки"):** Вкладка `SettingsTab.tsx` позволяет управлять профилем, а также создавать и удалять **промокоды** и использовать свою **партнерскую ссылку**.

### 🤝 Социальные и Коммерческие Экосистемы

CryptoCraft — это больше, чем просто витрина. Это живая среда для взаимодействия и торговли.

#### 1. Центр Сообщества и Live-commerce
-   **Что это?** Платформа для общения и "живых" продаж.
-   **Как это работает?** В "Центре сообщества" (`CommunityHubPage.tsx`) есть полноценный **форум** для обсуждений. Кроме того, Pro-продавцы теперь могут проводить **прямые эфиры**.
-   **Статус функции: ПОЛНОСТЬЮ РЕАЛИЗОВАНО И РАСШИРЕНО.** Pro-продавцы получили доступ к **профессиональному центру управления трансляцией** (`CreateLiveStreamPage.tsx`). Теперь, помимо базового запуска, доступны расширенные настройки:
    *   **Продвинутая модерация:** Можно назначить другого пользователя **модератором** чата или активировать платного **AI-модератора** за 3 USDT, который будет автоматически фильтровать спам.
    *   **Планирование:** Можно **запланировать трансляцию** на определенную дату и время, создавая анонс для подписчиков.
    *   **Вовлечение аудитории:** Можно добавить **приветственное сообщение**, которое автоматически появится в чате в начале эфира.
-   **В чем ценность?** Это превращает Live-commerce из простой функции в мощный инструмент для профессиональных продаж и маркетинга, повышая вовлеченность, создавая комьюнити и стимулируя импульсивные покупки.

#### 2. Центр Разрешения Споров
-   **Что это?** Безопасное пространство для решения конфликтных ситуаций.
-   **Как это работает?** Если возникает проблема с заказом, покупатель или продавец могут открыть спор. На странице `DisputeCenterPage.tsx` они ведут диалог, прикрепляют доказательства, а симулированный **арбитр** помогает найти решение.
-   **В чем ценность?** Ключевой элемент для построения доверия. Пользователи знают, что их сделки защищены.

#### 3. DAO (Децентрализованное управление)
-   **Что это?** Система, позволяющая Pro-продавцам напрямую влиять на развитие платформы.
-   **Как это работает?** В "Центре управления DAO" (`GovernancePage.tsx`) Pro-продавцы могут просматривать предложения по улучшению платформы (например, изменение комиссии) и голосовать "за" или "против" на странице `ProposalDetailPage.tsx`.
-   **В чем ценность?** Воплощает принципы Web3, делая сообщество реальным участником управления и повышая лояльность самых активных продавцов.

#### 4. Маркетплейс Услуг
-   **Что это?** Расширение платформы, позволяющее продавать не только товары, но и услуги.
-   **Как это работает?** Продавцы могут создавать объявления типа "Услуга" (например, "дизайн на заказ" или "консультация"). Интерфейс адаптирован для корректного отображения и заказа таких предложений.
-   **В чем ценность?** Превращает CryptoCraft в полноценную платформу для креативной экономики, открывая новые источники дохода для мастеров.

#### 5. Логистический Хаб (Автоматизация Доставки)
-   **Что это?** Интегрированная система, которая делает процесс доставки простым и прозрачным.
-   **Как это работает?**
    1.  **Авто-расчет:** При оформлении заказа (`CheckoutPage.tsx`) стоимость доставки рассчитывается автоматически на основе веса товаров и выбранного перевозчика.
    2.  **Авто-генерация ТТН:** В профиле продавца (`SalesTab`) кнопка "Сгенерировать ТТН" симулирует создание накладной, меняет статус заказа на "Отправлен" и добавляет трек-номер.
    3.  **Интерактивное отслеживание:** Покупатель в своем профиле (`PurchasesTab`) может отслеживать движение посылки на наглядной временной шкале.
-   **В чем ценность?** Повышает профессионализм платформы и доверие пользователей, делая доставку предсказуемой и удобной.

#### 6. Инструменты для B2B (Оптовые продажи)
-   **Что это?** Функционал для бизнес-клиентов.
-   **Как это работает?** Продавцы могут включить оптовые продажи для своих товаров, установив минимальное количество и специальную цену. На странице товара (`ProductDetailPage.tsx`) покупатели могут легко переключаться между розничным и оптовым режимами.
-   **В чем ценность?** Расширяет рынок сбыта для продавцов и привлекает на платформу новый сегмент покупателей.

#### 7. Эскроу на Смарт-контрактах (Симуляция)
-   **Что это?** Максимально безопасный способ проведения сделок.
-   **Как это работает?** При выборе этого способа оплаты (`CheckoutPage.tsx`) средства покупателя "замораживаются" не на счете платформы, а на независимом цифровом контракте. Деньги автоматически переводятся продавцу только после того, как покупатель подтвердит получение товара.
-   **В чем ценность?** Даже в виде симуляции, это демонстрирует высочайший уровень безопасности и технологичности, подготавливая платформу к полной интеграции с блокчейном.

---

## Концепция развития: "CryptoCraft Evolution" (Roadmap)

Стратегический план по превращению CryptoCraft в полноценную, интеллектуальную и децентрализованную **экономическую экосистему**.

### Фаза 1: Укрепление Ядра и Доверия (ЗАВЕРШЕНО)
Все функции, описанные в разделе "Реализованная функциональность", относятся к этой фазе. Мы создали надежный, многофункциональный и технологически продвинутый маркетплейс.

### Фаза 2: Экосистема и Масштабирование (ЗАВЕРШЕНО)
Мы вышли за рамки просто "купи-продай", построив полноценную экономическую среду с логистикой, B2B, услугами, DAO и безопасными сделками на смарт-контрактах.

### Фаза 3: Инструменты для Pro-продавцов и Масштабирование (План)
-   **Цель:** Предоставить мощные инструменты для продавцов с большим объемом товаров, чтобы упростить управление и миграцию на CryptoCraft.
-   **ПЛАН:**
    -   **Массовая загрузка и управление товарами (CSV/Excel):** Реализовать функцию импорта/экспорта товаров через стандартизированные файлы. Продавец сможет скачать шаблон, заполнить его и загрузить сотни товаров за один раз. Бэкенд будет обрабатывать эти файлы в фоновом режиме.
    -   **Импорт объявлений с других платформ (AI-Scraping):** Разработка интеллектуального инструмента для автоматического переноса магазина продавца с любого публичного маркетплейса.
        -   **Цель:** Устранить главный барьер для миграции продавцов — необходимость вручную создавать десятки или сотни объявлений заново.
        -   **Пользовательский сценарий (Workflow):**
            1.  **Вставка URL:** Продавец вставляет список ссылок на свои товары в специальный интерфейс.
            2.  **Сбор данных (Scraping):** Бэкенд-сервис (serverless-функция) получает URL, скачивает HTML-код каждой страницы и передает его на фронтенд. Это необходимо для обхода ограничений CORS.
            3.  **AI-анализ и извлечение:** Фронтенд отправляет полученный HTML в Google Gemini API с промптом на извлечение всей информации о товаре (заголовок, описание, цена, URL изображений, характеристики) в структурированном JSON-формате. AI выступает в роли универсального парсера, который адаптируется к разметке любого сайта.
            4.  **Проверка и редактирование:** Пользователю выводится список "черновиков", созданных AI. Он может быстро проверить корректность данных, внести правки в любое поле и удалить ненужные товары.
            5.  **Групповая публикация:** Одной кнопкой "Опубликовать все" запускается процесс, который автоматически скачивает изображения по URL, загружает их на наш сервер (Cloudinary) и создает все объявления на CryptoCraft.
        -   **Ключевые технологии:** React, Serverless Function, Cheerio (для парсинга HTML на сервере), Google Gemini API.

### Фаза 4: Вершина Децентрализации и Платформенная Независимость (Будущее видение)
-   **ПЛАН: Полная интеграция с TON:** Переход от симуляции к реальному взаимодействию со смарт-контрактами и NFT в сети TON.
-   **ПЛАН: Платформенная независимость:** Разработка **standalone-приложений** для Mobile (iOS/Android), Desktop и Web для снижения зависимости от экосистемы Telegram.
-   **ПЛАН: Эволюция аутентификации:** Переход от аутентификации по `Telegram ID` к системе на основе **номера телефона и пароля/PIN-кода**.
-   **ПЛАН: Развитие крипто-интеграции:** Рассмотрение **встроенного кастодиального кошелька** и **интеграция с API бирж**.

---

## Техническая Архитектура и План Развертывания

Для перехода от симуляции к полноценному продукту необходима серверная часть и инструменты администрирования.

### Разработка Бэкенда (Поэтапный план)

-   **Фаза 1: Фундамент (ЗАВЕРШЕНО)**
    -   **Статус:** ✅ Завершено.
    -   **Результат:** Создана базовая структура бэкенд-приложения на NestJS (`backend/`). Настроен `package.json`, `tsconfig.json`, и заложены основные модули (`AppModule`, `AppController`, `AppService`). Приложение успешно запускается и отвечает на тестовый запрос.

-   **Фаза 2: База данных и первые сущности (ЗАВЕРШЕНО)**
    -   **Статус:** ✅ Завершено.
    -   **Результат:** Настроено подключение к PostgreSQL. Созданы сущности (ORM-модели) для `User` и `Product`, а также базовая структура модулей для управления ими. TypeORM успешно синхронизирует модели с БД, создавая таблицы.

-   **Фаза 3: Реализация CRUD API (ЗАВЕРШЕНО)**
    -   **Статус:** ✅ Завершено.
    -   **Результат:** Реализованы базовые операции (Create, Read, Update, Delete) для пользователей и товаров. Созданы DTO для валидации входящих данных. Наполнены контроллеры и сервисы логикой для взаимодействия с БД.

-   **Фаза 4: Интеграция Фронтенда и Бэкенда (В РАБОТЕ)**
    -   **Статус:** 🚧 В работе.
    -   **Задача:** Заменить mock-сервис на фронтенде на реальные API-вызовы к бэкенду. Включить CORS на бэкенде.

-   **Фаза 5: Аутентификация и остальные модули (План)**
    -   **Задача:** Реализовать JWT-аутентификацию, а затем последовательно создавать модули для заказов, чатов, коллекций и т.д., постепенно отключая соответствующие части в `apiService.ts` на фронтенде.

### Платформа развертывания: Render.com

Render.com выбран как оптимальный сервис для развертывания проекта благодаря своей простоте, масштабируемости и поддержке необходимой архитектуры. Структура проекта на Render будет выглядеть следующим образом:

-   **Database (PostgreSQL):** Управляемый экземпляр PostgreSQL для хранения всех данных приложения.
-   **Backend (Web Service):** Приложение на Node.js (например, NestJS/Express), которое будет служить API для всех клиентских приложений.
-   **Frontend (Static Site):** Основное клиентское React-приложение для пользователей.
-   **Admin Panel (Static Site):** Отдельное React-приложение для администрирования, которое будет обращаться к тому же Backend API, что и основной фронтенд.

### Архитектура Фронтенда: Управление состоянием и Аутентификация

-   **Управление состоянием сервера (Server State):** Для взаимодействия с бэкендом будет использована библиотека **React Query (TanStack Query)**.
    -   **Принцип:** React Query — это **клиентская библиотека**, а не сторонний сервер. Она работает внутри нашего React-приложения и является "умным" менеджером данных.
    -   **Задачи:**
        1.  **Устранение "ручного" управления:** Заменяет сложную логику на `useState` и `useEffect` для отслеживания состояний загрузки, ошибок и данных.
        2.  **Кэширование:** Хранит полученные от бэкенда данные в кэше, что предотвращает лишние запросы и ускоряет работу интерфейса.
        3.  **Автоматическое обновление:** Может автоматически обновлять данные в фоновом режиме, гарантируя их актуальность.
    -   **Независимость:** Этот инструмент не создает зависимости от чужих серверов. Он будет общаться **исключительно с нашим собственным Backend API**, обеспечивая полный контроль над данными и инфраструктурой.

-   **Стратегия Аутентификации:** Принята гибкая, двухэтапная стратегия, позволяющая легко эволюционировать от Telegram Web App к полноценному независимому приложению.
    -   **Этап 1 (Текущий - TWA):** Аутентификация основана на данных пользователя, предоставляемых Telegram (`WebAppUser`). Бэкенд будет доверять этому ID для идентификации.
    -   **Этап 2 (Будущий - Standalone):** Будет реализована классическая система аутентификации по номеру телефона и паролю/PIN-коду с использованием **JWT (JSON Web Tokens)**.
    -   **Гибкость архитектуры:** Хук `useAuth` служит **абстрактным слоем**. Вся логика получения пользователя инкапсулирована внутри него. Для перехода с этапа 1 на этап 2 потребуется изменить только внутреннюю реализацию `useAuth` и `apiService` (для добавления токенов в заголовки). Остальные компоненты приложения останутся без изменений.
    -   **Процесс миграции пользователей:** Переход будет управляемым и бесшовным. В админ-панели будет реализован специальный "переключатель". При его активации (например, за 2 недели до запуска standalone-приложения) на бэкенде будет включен соответствующий режим. На фронтенде, в личном кабинете каждого пользователя, появится специальный раздел для ввода номера телефона и установки пароля/PIN-кода. Таким образом, существующая база пользователей сможет заранее привязать новые учетные данные к своим Telegram ID, что обеспечит плавный переход на новую систему аутентификации без потери данных и необходимости перерегистрации.

### Архитектура Бэкенда и Процесс Оплаты

Архитектура бэкенда будет централизованной, с прямой интеграцией для верификации блокчейн-транзакций, которые инициируются на стороне клиента.

1.  **Централизованный Бэкенд (Node.js + NestJS, PostgreSQL)**
    -   **Задачи:**
        -   **База данных:** Хранение информации о пользователях, товарах, заказах, чатах и т.д.
        -   **API:** Предоставление защищенных эндпоинтов для фронтенда, которые заменят mock-сервис `apiService.ts`.
        -   **Безопасные запросы к внешним сервисам:** Все вызовы к Gemini AI и Cloudinary будут производиться с сервера, надежно защищая API-ключи.
        -   **Аутентификация:** Управление пользовательскими сессиями.
        -   **Верификация транзакций в TON:** Ключевая роль бэкенда в крипто-платежах. Сервер будет получать хэш транзакции от клиента и проверять ее непосредственно в блокчейне TON через публичные API (например, Toncenter).

2.  **Процесс Оплаты (Клиентская сторона + Блокчейн)**
    -   **Инициация на клиенте:** Оплата в TON или USDT инициируется пользователем непосредственно на фронтенде. Всплывающее окно `WalletModal` (используя `@tonconnect/ui-react`) предлагает пользователю подключить свой кошелек (например, Tonkeeper).
    -   **Подписание транзакции:** Пользователь подтверждает и подписывает транзакцию в своем кошельке. Фронтенд-приложение никогда не получает доступ к приватным ключам.
    -   **Отправка и верификация:**
        1.  Кошелек пользователя отправляет транзакцию в сеть TON.
        2.  Фронтенд получает подтверждение и хэш транзакции.
        3.  Этот хэш отправляется на наш бэкенд.
        4.  Бэкенд обращается к API сети TON, находит транзакцию по хэшу и проверяет ее параметры (сумма, получатель).
        5.  После успешной верификации бэкенд обновляет статус заказа в базе данных PostgreSQL на "Оплачен".
    -   **Безопасность и Децентрализация:** Этот подход является стандартом для Web3-приложений. Сама транзакция децентрализована и безопасна (контролируется пользователем), а наша система лишь проверяет ее результат (централизованная верификация).

### Масштабируемость и Высокая Нагрузка (High Availability & Scaling)

Для обеспечения стабильной работы при росте числа пользователей до миллионов, архитектура бэкенда будет изначально спроектирована для **горизонтального масштабирования**.

-   **Принцип:** Вместо одного мощного сервера (вертикальное масштабирование), мы будем использовать множество менее мощных, но идентичных копий нашего бэкенд-приложения, распределяя нагрузку между ними.
-   **Ключевые компоненты:**
    1.  **Балансировщик нагрузки (Load Balancer):** Это "диспетчер", который будет стоять перед всеми нашими серверами. Все запросы от пользователей сначала приходят к нему, а он уже решает, какому из свободных серверов передать задачу. Это обеспечивает равномерное распределение нагрузки и отказоустойчивость: если один сервер выходит из строя, балансировщик просто перестает направлять на него трафик, и приложение продолжает работать.
    2.  **Автомасштабирование (Autoscaling):** Платформа Render.com позволяет настроить правила автоматического масштабирования. Например: "Если средняя загрузка CPU на серверах превышает 70%, автоматически добавить новый сервер в пул. Если нагрузка падает — убрать лишний". Это позволяет эластично адаптироваться к пиковым нагрузкам без переплаты за простаивающие мощности.
    3.  **Stateless-архитектура бэкенда:** Критически важно, чтобы наши серверы не хранили локально данные о сессиях пользователей. Вся информация (кто залогинен, что в корзине) должна храниться в централизованном хранилище (база данных PostgreSQL или кэш Redis), чтобы пользователь мог при каждом новом запросе попасть на любой из серверов и продолжить работу без проблем.
    4.  **Масштабируемая база данных:** Управляемый сервис PostgreSQL на Render.com также позволяет легко масштабироваться: можно увеличить мощность инстанса (вертикальное масштабирование) или, при необходимости, настроить реплики для чтения (горизонтальное) для снижения нагрузки на основную базу.

Этот подход гарантирует, что CryptoCraft будет готов к экспоненциальному росту пользовательской базы, обеспечивая высокую доступность и быструю работу приложения.

### Административная панель (React + shadcn/ui)

Админ-панель будет представлять собой отдельное веб-приложение для управления платформой. Использование **shadcn/ui** позволит быстро создать профессиональный и удобный интерфейс.

-   **Основные разделы:**
    -   **Главный дашборд:** Ключевые метрики (количество пользователей, объем продаж, активные споры).
    -   **Управление пользователями:** Просмотр, поиск, блокировка, ручное присвоение Pro-статуса.
    -   **Управление товарами:** Модерация (одобрение/отклонение новых объявлений), просмотр всех товаров.
    -   **Управление заказами:** Просмотр всех транзакций на платформе.
    -   **Центр споров:** Интерфейс для арбитра для ведения и разрешения споров.
    -   **Настройки платформы:** Управление категориями, размером комиссии и другими глобальными параметрами.